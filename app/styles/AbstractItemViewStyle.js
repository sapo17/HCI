import Color from '../config/Color';
import GlobalStyles from './GlobalStyles';

export default {
  overlayStyle: [
    GlobalStyles.BackgroundColor, GlobalStyles.Opacity
  ],
  buttonColor: Color.SECONDARY_FOREGROUND
}